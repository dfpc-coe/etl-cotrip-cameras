{
    "version": "1.0",
    "name": "COtrip Traffic Cameras",
    "description": "Ingests traffic camera locations and imagery metadata from the Colorado COtrip system and posts them to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "COtrip Cameras posts traffic camera locations to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls the Colorado COtrip system for updated traffic camera data on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(5 minutes)"
                }
            }
        }
    }
}
